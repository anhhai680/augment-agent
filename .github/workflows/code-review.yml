name: Auto-review PRs
on:
  pull_request:
    types: [opened, synchronize]
    branches:
      - main

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Create instruction file
        env:
          PR_NUMBER: ${{ github.event.pull_request.number }}
          REPOSITORY: ${{ github.repository }}
          BASE_BRANCH: ${{ github.event.pull_request.base.ref }}
          HEAD_BRANCH: ${{ github.event.pull_request.head.ref }}
        run: |
          cat > /tmp/openai-review-instruction.txt << EOF
          Perform a comprehensive code review of the following pull request using OpenAI GPT-4:

          **Pull Request Information:**
          - PR Number: ${PR_NUMBER}
          - Repository: ${REPOSITORY}
          - Base Branch: ${BASE_BRANCH}
          - Head Branch: ${HEAD_BRANCH}

          **Review Focus:**
          Analyze the modified files and provide detailed feedback on:
          - Code quality and adherence to best practices
          - Potential bugs, errors, or security vulnerabilities
          - Performance implications of the changes
          - Suggestions for improvement or optimization
          - Any missing error handling or edge cases
          - Code maintainability and readability
          - OpenAI-specific considerations (AI safety, bias, etc.)

          **Output Format:**
          Please provide your review in the following JSON format for better organization:

          ```json
          {
            "summary": "Overall review summary and general comments",
            "files": {
              "path/to/file.js": {
                "line_comments": [
                  {
                    "line": 42,
                    "comment": "Specific feedback for this line",
                    "side": "RIGHT"
                  }
                ],
                "general_comments": ["General feedback about this file"]
              }
            }
          }
          ```

          - Use specific line numbers for targeted feedback
          - Include both line-specific comments and general file feedback
          - Provide specific, actionable feedback with file and line references
          - Focus on the actual code changes and their impact on the codebase

          Please post your review as a review comment on the PR. Do not approve or request changes.
          EOF

      - name: Generate PR Review
        uses: ./
        with:
          llm_provider: "openai"
          llm_api_key: ${{ secrets.OPENAI_API_KEY }}
          model: "gpt-4o-mini"
          llm_temperature: "0.3"
          llm_max_tokens: "4000"
          post_comment: "true"
          comment_type: "review"
          review_event: "COMMENT"
          use_inline_comments: "true"
          inline_comment_strategy: "review_with_comments"
          pull_number: ${{ github.event.pull_request.number }}
          repo_name: ${{ github.repository }}
          github_token: ${{ secrets.CODEREVIEW_GITHUB_TOKEN }}
          instruction_file: /tmp/openai-review-instruction.txt
